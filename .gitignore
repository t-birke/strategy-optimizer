node_modules/
*.duckdb
*.duckdb.wal

# macOS Finder artifact
.DS_Store

# Runtime fitness cache — per-(spec, dataset) JSON files regenerated by the
# GA on each run. Not source; can get large.
data/fitness-cache/

# Generated Pine indicators — emitted by scripts/pine-deploy.js / pine-export.js
# from the optimizer winners. Reproducible from spec + gene, no need to track.
pine/generated/
